/**
 * Placeholder for generated Supabase types. Once the project is linked,
 * regenerate with:
 *   supabase gen types typescript --linked > packages/shared/src/types/database.ts
 * Kept as `any`-shaped until then so the rest of the codebase can import
 * `Database` without breaking.
 */
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export type Database = Record<string, unknown>;
